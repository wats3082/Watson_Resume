{
  "name": "russell-watson-portfolio",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace portfolio",
    "build": "npm run build --workspace portfolio",
    "lint": "npm run lint --workspace portfolio",
    "preview": "npm run preview --workspace portfolio"
  }
}
